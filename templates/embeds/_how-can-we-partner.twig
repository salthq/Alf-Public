{# How Can We Partner Band #}
{% set partnerInfo = craft.entries.section('howCanWePartner').one() %}
{# Heading #}
<p class="mx-auto mt-5 max-w-prose font-rubik uppercase tracking-tight text-4xl text-center pt-24">
  {{ partnerInfo.howCanWePartnerTitle }}
</p>
{# End | Heading #}

{# Section 1 - Sponsorship #}
<section class="mx-10">
  <div class="my-5 lg:mx-auto max-w-6xl z-10 px-4 sm:px-6 lg:px-8 md:bg-alf-nav-blue pt-5 md:bg-opacity-10 rounded-3xl">
    
    {# Heading #}
      {% set numberOne = partnerInfo.numberOne.one() %}
      <div class="flex flex-col py-10 col-span-1 lg:col-span-3 ">
        <div>
          <hr class="bg-alf-dark-blue h-0.5 border-0 md:w-1/2" />
        </div>
        <div class="flex flex-row my-5 mx-2 items-start">
          <div class="flex flex-col">
            <div class="flex-grow">
              <img src="{{ numberOne.url }}" alt="{{ numberOne.alt }}" />
            </div>
            
          </div>

          <div class="flex-1 px-5 text-2xl font-rubik my-5">
            <h1>
              {{ partnerInfo.stepOneHeading }}
            </h1>
          </div>
        </div>
        <div>
          <hr class="bg-alf-dark-blue h-0.5 border-0 md:w-1/2" />
        </div>
      </div>
      {# End | Heading #}
    
    {# Programme Grid #}
    <div class="grid grid-cols-1 md:gap-y-10 gap-y-4 lg:grid-cols-3 lg:gap-y-0 lg:gap-x-4">
      

      {% set programmes = partnerInfo.programmeList.all() %}
      {# Items #}
      {% for program in programmes %}
        <a  href="#contact"  @click="contactModal = true" class="group flex flex-col bg-alf-white rounded-2xl shadow-xl lg:my-10 my-5 mx-2">
          <div class="flex-1 relative pt-16 px-6 pb-8 md:px-8">
            <div class="absolute top-0 p-5 inline-block bg-alf-dark-blue rounded-xl shadow-lg transform -translate-y-1/2">
              {% set image = program.programmeImage.one() %}
              <img class="h-6 w-6 text-white" src="{{ image.url }}" alt="{{ image.alt }}" />
            </div>
            <h3 class="text-xl font-medium font-rubik">
              {{ program.programName }}
            </h3>
            <br>
            <p class="pt-2 text-sm font-medium font-rubik">
              {{ program.programmeSubtitle }}
            </p>
           
          </div>
        
        </a>
      {% endfor %}

      {# End | Items #}
    </div>

    <div class="pb-5 flex">
      <a 
          href="#connect-with-us" 
          class="mx-auto py-2 px-10 font-rubik rounded-md shadow-sm border-solid border-2 bg-alf-blue border-alf-nav-blue hover:bg-alf-nav-blue"     >
          Connect with us
      </a>
    </div>
    {# End | Programme Grid #}
  </div>
</section>
{# End | Section 1 - Sponsorship #}

{# Section 2 - Allocate SD spend #}
<section class="ml-10 lg:mx-10">
  <div class="my-20 max-w-6xl lg:mx-auto z-10 px-4 sm:px-6 lg:px-8 bg-alf-white rounded-l-3xl rounded-r-none lg:rounded-r-3xl">
    {# Programme Grid #}
    <div class="grid grid-cols-1 lg:grid-cols-3 lg:grid-rows-2 lg:gap-x-4 xl:-translate-x-24">
      <div class="invisible h-0 lg:h-auto lg:visible col-span-1 row-span-2">
        {% set jump = partnerInfo.howCanWePartnerImage.one() %}
        <img src="{{ jump.url }}" alt="{{ jump.alt }}" />
      </div>

      {# Heading #}
      {% set numberTwo = partnerInfo.numberTwo.one() %}
      <div class="flex flex-col py-10 col-span-1 lg:col-span-2">
        <div>
          <hr class="bg-alf-dark-blue h-0.5 border-0" />
        </div>
        <div class="flex flex-row my-5 mx-2 items-start">
          <div class="flex flex-col">
            <div class="flex-grow">
              <img src="{{ numberTwo.url }}" alt="{{ numberTwo.alt }}" />
            </div>
            <div class="flex-shrink lg:hidden lg:h-0 -translate-x-8">
              <img src="{{ jump.url  }}" class="max-h-56" alt="{{ jump.alt }}" />
            </div>
          </div>

          <div class="flex-1 px-5 text-2xl font-rubik">
            <h1>
              {{ partnerInfo.stepTwoHeading }}
            </h1>
          </div>
        </div>
        <div>
          <hr class="bg-alf-dark-blue h-0.5 border-0" />
        </div>
      </div>
      {# End | Heading #}

      <div class="col-span-2 ">
        <p class="text-lg">Designed for academically advanced learners, the IEB’s Further Studies subjects (also known as Advanced Programme) are set to improve self-discipline and critical thinking skills. Because of this, the IEB Further Studies subjects are the ideal way for learners to give themselves a competitive edge in their chosen degree programmes.</p>
        <div class="text-lg">Contact Us</div>
      </div>

    </div>
  </div>
  {# End | Programme Grid #}
</section>
{# End | Section 2 - Allocate SD spend #}

{# Section 2 - Tertiary Scholarship #}
<section class="ml-10 lg:mx-10">
  <div class="my-20 max-w-6xl lg:mx-auto z-10 px-4 sm:px-6 lg:px-8 bg-alf-white rounded-l-3xl rounded-r-none lg:rounded-r-3xl">
    {# Programme Grid #}
    <div class="grid grid-cols-1 lg:grid-cols-3 lg:gap-x-4">
      

      {# Heading #}
      {% set numberTwo = partnerInfo.numberTwo.one() %}
      <div class="flex flex-col py-20 col-span-1 lg:col-span-3">
        <div>
          <hr class="bg-alf-dark-blue h-0.5 border-0" />
        </div>
        <div class="flex flex-row my-10 mx-2 items-start">
          <div class="flex flex-col">
            <div class="flex-grow">
              <img src="{{ numberTwo.url }}" alt="{{ numberTwo.alt }}" />
            </div>
            
          </div>

          <div class="flex-1 px-5 text-2xl font-rubik">
            <h1>
              {{ partnerInfo.stepTwoHeading }}
            </h1>
          </div>
        </div>
        <div>
          <hr class="bg-alf-dark-blue h-0.5 border-0" />
        </div>
      </div>
      {# End | Heading #}

       {% set programmes = partnerInfo.fsList.all() %}
      {# Items #}
      {% for program in programmes %}
        <a  href="#contact"  @click="contactModal = true" class="group flex flex-col bg-alf-white rounded-2xl shadow-xl my-10 lg:my-5 mx-2">
          <div class="flex-1 relative pt-16 px-6 pb-8 md:px-8">
            <div class="absolute top-0 p-5 inline-block bg-alf-dark-blue rounded-xl shadow-lg transform -translate-y-1/2">
              {% set image = program.courseImage.one() %}
              <img class="h-6 w-6 text-white" src="{{ image.url }}" alt="{{ image.alt }}" />
            </div>
            <h3 class="text-xl font-medium font-rubik">
              {{ program.courseName }}
            </h3>
            <br>
            <p class="pt-2 text-sm font-medium font-rubik text-alf-dark-blue">
              Quotation available
            </p>
           
          </div>
          <div class="p-6 bg-alf-blue rounded-bl-2xl rounded-br-2xl md:px-8">
            <div class="text-base font-medium font-rubik group-hover:underline">
              Contact us<span aria-hidden="true">&rarr;</span>
            </div>
          </div>
        </a>
      {% endfor %}
    </div>
  </div>
  {# End | Programme Grid #}
</section>
{# End | Section 2 - Tertiary Scholarship #}





{# Section 4 - Information #}
<section class="px-10">
  <div class="my-10 max-w-6xl lg:mx-auto z-10 px-4 sm:px-6 lg:px-8 bg-alf-nav-blue pt-5 bg-opacity-10 rounded-3xl">
    {# Content Grid #}
    <div class="grid grid-cols-1 gap-y-10 lg:grid-cols-12 items-center align-middle pb-5">
      <div class="col-span-8 font-rubik text-2xl font-thin px-3 text-left">
        <p>
          {{ partnerInfo.fundersContent }}
        </p>
        <br />
        <p>
          {{ partnerInfo.furtherDiscount }}
        </p>
      </div>
      {% set percentage = partnerInfo.fundersImage.one() %}
      <div class="col-span-4 row-start-1 lg:col-start-9 mx-auto">
        <img src="{{ percentage.url }}" alt="{{ percentage.alt }}" />
      </div>
    </div>

    <div class="font-rubik text-2xl font-semibold text-center">
      <hr class="bg-alf-dark-blue h-0.5 border-0" />
      <p class="p-8">
        {{ partnerInfo.getInvolvedContent }}
      </p>
    </div>
  </div>
  {# End | Content Grid #}
</section>
{# End | Section 4 - Information #}
<br />
{# End | How Can We Partner Band #}
