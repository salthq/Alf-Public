{# Who We Are Band #}
{# Styling Wrapper #}
<div>
  {% set whoWeAreStyling = '/images/WhoWeAre Styling.svg' %}
  <img src="{{ whoWeAreStyling }}"
    alt="Styling"
    class="invisible h-0 lg:visible lg:h-fit absolute" />

  {% set whoWeAreStylingMobile = '/images/WhoWeAreMobileStyling.svg' %}
  <img src="{{ whoWeAreStylingMobile }}"
    alt="Styling"
    class="visible h-auto lg:invisible lg:h-0 absolute" />

  {# Outer container #}
  <div class="mx-10 lg:pt-14 my-5">
    {# Inner container #}
    <div class="max-w-6xl mx-auto">
      <div class="lg:grid lg:grid-cols-12 lg:gap-8">
        {# Lock and Key Image #}
        {% set lockAndKey = '/images/lockAndKey.svg' %}

        <div class="mt-16 sm:mt-26 flex justify-center sm:max-w-lg sm:mx-auto lg:mt-0 lg:max-w-none lg:mx-0 lg:col-span-6 lg:flex lg:items-center lg:justify-center z-50 md:z-50 px-10 pb-10">
          <img src="{{ lockAndKey }}" alt="lock and key Image" />
        </div>
        {# End | Lock and Key Image #}

        <div class="lg:h-0 lg:invisible bg-alf-blue md:row-start-2 col-start-1 col-span-12 h-14 -translate-y-20 rounded-3xl"></div>

        <div class="text-left sm:text-center md:max-w-2xl md:mx-auto lg:col-span-6 lg:text-left">
          {# Title #}
          <h1 class="block font-rubik text-4xl">
            WHO WE ARE
          </h1>

          {# Text #}
          <p class="font-light font-rubik text-left sm:text-center sm:text-xl lg:text-left sm:mt-5 lg:block lg:pb-0 lg:text-lg">
            The Advantage Learn Foundation was formed under directive from
            Advantage Learn in order to leverage the resources and skills in
            Advantage Learn to drive impact and positive social change.
          </p>
          {# Text #}
          <p class="font-light font-rubik text-left sm:text-center sm:text-xl lg:text-left sm:mt-5 lg:block lg:pb-0 lg:text-lg">
            Our mission is to provide under-resourced learners and educators in
            Africa with access to high-quality digital education to enable them
            to become agents and not objects in the Fourth Industrial
            Revolution.
          </p>
        </div>

        <div class="invisible lg:h-24 lg:visible bg-alf-blue md:row-start-2 col-start-1 col-span-12 h-24 -translate-y-28 rounded-3xl"></div>
      </div>
    </div>
    {# End | Inner container #}
  </div>
  {# End | Outer container #}
  {# End | Who We Are Band #}
</div>
{# End | Styling Wrapper #}
